<view class="list">
  <block class="item" wx:for="{{todoList}}" wx:for-item="todo" wx:key="{{index + 1}}">
    <todoItem text="{{todo.text}}" completed="{{todo.completed}}"></todoItem>
  </block>
</view>