<view class="list">
  <block class="item" wx:for="{{todoList}}" wx:for-item="todo" wx:key="unique">
    <todoItem id="{{todo.id}}" text="{{todo.text}}" completed="{{todo.completed}}" bind:onToogle="onToogle" />
  </block>
</view>